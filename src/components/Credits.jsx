import React from 'react';

export default function Credits() {
  return (
    <div className="w-full flex items-center justify-center py-8 text-white/60 text-sm">
      <p>
        Image sources use Unsplash placeholders — replace URLs to customize. Built with responsive CSS, masked edges, and true infinite looping.
      </p>
    </div>
  );
}
